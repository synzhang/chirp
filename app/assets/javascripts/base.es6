;((root) => {
  root.App = {
    Utils: {},
    Common: {},
    Components: {},
    Mixins: {},
    Models: {},
    Collections: {},
    Views: {},
    Pages: {},
  };

  root.$window = $(window)
  root.$document = $(document)
  root.$html = $('html')
  root.$body = $('body')
}(window))
