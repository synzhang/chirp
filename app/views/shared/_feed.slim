- if @feed_items.any?
  ol.microposts
    = render partial: 'shared/list_item', collection: @feed_items, as: :item, locals: {item_partial: 'microposts/micropost', item_name: :micropost}
  = will_paginate @feed_items
